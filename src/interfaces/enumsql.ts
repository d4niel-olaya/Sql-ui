

enum columnTypes
{
    BIGINT = 'bigint',
    INT = 'int',
    VARCHAR = 'varchar',
    DATE = 'date',
    FLOAT = 'float',
    BOOLEAN = 'boolean',
    TEXT = 'text',
    DECIMAL = 'decimal',
    CHAR = 'char',
}


enum columnConstraints
{
    NOT_NULL = 'NOT NULL',
    UNIQUE = 'UNIQUE',
    PRIMARY_KEY = 'PRIMARY KEY',
    FOREIGN_KEY = 'FOREIGN KEY',
}